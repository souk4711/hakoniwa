"@include" = [
  "abstractions/base.toml",
]

mounts = [
  { source = "{{ HOME }}/.local/share/hakoniwa/apps/fish", destination = "{{ HOME }}", rw = true },
]

envs = [
  { name = "HOME" },
  { name = "TERM" },
]

[command]
cmdline = ["/bin/fish"]
cwd = "{{ HOME }}"
